{
  "setup-worktree-unix": [
    "bun run install:workspace",
    "bun run build:scms",
    "bun install --cwd platform/scms",
    "bun run --cwd platform/scms build"
  ],
  "setup-worktree": [
    "bun run install:workspace",
    "bun run build:scms",
    "bun install --cwd platform/scms",
    "bun run --cwd platform/scms build"
  ]
}
