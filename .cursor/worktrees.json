{
  "setup-worktree-unix": [
    "bun run install:workspace",
    "bun run build:scms",
    "bun --cwd platform/scms install",
    "bun --cwd platform/scms run build"
  ],
  "setup-worktree": [
    "bun run install:workspace",
    "bun run build:scms",
    "bun --cwd platform/scms install",
    "bun --cwd platform/scms run build"
  ]
}
