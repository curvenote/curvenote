{
  "setup-worktree-unix": [
    "bun install",
    "bun run build",
    "bun --cwd platform/scms run build"
  ],
  "setup-worktree": [
    "bun install",
    "bun run build",
    "bun --cwd platform/scms run build"
  ]
}
