{
  "setup-worktree-unix": [
    "bun install",
    "bun run build:scms",
    "bun --cwd platform/scms install",
    "bun --cwd platform/scms run build"
  ],
  "setup-worktree": [
    "bun install",
    "bun run build:scms",
    "bun --cwd platform/scms install",
    "bun --cwd platform/scms run build"
  ]
}
