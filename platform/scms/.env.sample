VITE_PORT=3031
APP_CONFIG_ENV=development
VITE_APP_CONFIG_ENV=development
VITE_APP_CONFIG_DIRECTORY='.'
VITE_APP_CONFIG_SCHEMA_DIRECTORY='../../'
DATABASE_URL=postgresql://journals:curvenote@localhost:5432/journals?statement_cache_size=0
# Set PUBSUB_EMULATOR_HOST to route Pub/Sub through the local emulator
# (see docs/jobs/dx-local-pubsub.md). Without this, dev mode uses HTTP stubs.
#PUBSUB_EMULATOR_HOST=localhost:8085
# Emulator + Pub/Sub client default to project curvenote-dev-1 (override if needed):
#PUBSUB_PROJECT_ID=curvenote-dev-1

# Local GCS emulator (see ../../docker-compose.gcs.yml and docs/storage/dx-local-gcs.md).
#STORAGE_EMULATOR_HOST=http://127.0.0.1:4443
#GCS_SIGNED_URL_ORIGIN=http://127.0.0.1:4443
