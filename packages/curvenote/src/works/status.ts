export async function status() {
  // report on status of current working folder
  // no transfer.yml
  // transfer.yml with work_id
  // transfer.yml with submission_id
  // transfer.yml with submission_id and submission in sync with latest work_version_id
}
