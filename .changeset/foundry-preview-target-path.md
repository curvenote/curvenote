---
'@curvenote/scms-doc-preview': patch
'@curvenote/scms': patch
---

Allow document preview fetch handlers to scope phase A/B work to an optional `targetPath` (main manuscript) instead of always processing every preview candidate
