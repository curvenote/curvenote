---
'@curvenote/common': minor
'@curvenote/task-converter': minor
'@curvenote/scms-server': minor
'@curvenote/scms-core': patch
---

Add `myst-curvenote-web` conversion type, grant system admins `app:works:web-article-generation` by default, and pass `invoked_by_id` through job `onJobPatch`.
