model DnsRouter {
  id            String  @id
  cdn           String
  cdn_key       String
  domain        String
  is_team       Boolean
  owner         String
  date_created  String
  date_modified String
  created_by    User    @relation(fields: [created_by_id], references: [id])
  created_by_id String

  @@index([domain, date_created(sort: Desc)])
}
